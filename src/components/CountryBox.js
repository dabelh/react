import React from 'react';

export default function CountryBox({name,area,population,flag}) {
    
	return (<div>
            <countryDits dits={name,area,population}
            <countryFlag flag={flag}
    }
    </div>

	)
}
